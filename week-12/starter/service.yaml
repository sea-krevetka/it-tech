apiVersion: v1
kind: Service
metadata:
  name: items-svc
  labels:
    app: items-app
    project_code: items-s01
spec:
  selector:
    app: items-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8181
  type: ClusterIP