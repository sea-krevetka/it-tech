type Item {
  id: ID!
  name: String!
  description: String
  price: Float!
  sku: String!
  created_at: String!
}

type Query {
  items: [Item!]!
  item(id: ID!): Item
}

type Mutation {
  createItem(name: String!, description: String, price: Float!, sku: String!): Item!
}